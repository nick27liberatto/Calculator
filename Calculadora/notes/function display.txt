not working

function resetDisplay () {
    operators.dividing = true
    //verifying if all operations are off
    let ok = 0
    for (let i = 0; i < allOperations.length; i++) {
        if (allOperations[i] == false) {
            ok++
        }
    }

    //if all operation are off, get event listener click off every button in dom
    if (ok == allOperations.length) {
        document.addEventListener("click", (evt) => {
            if(evt.target.matches('button')) {
                alert("worked")
            }
        })
    }
}



function clearCalc () {
    document.addEventListener("click", (evt) => {
        if(evt.target.matches('button') && elements.calculated == true) {
            show.innerHTML = ''
        }
    })
}